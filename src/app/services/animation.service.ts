export class AnimationService {
    // This Service will run certain animations it will need variables such as the item to be animated's DOM reference
    constructor() { }

    forward(ref){

    }

    back(ref){
        
    }
}