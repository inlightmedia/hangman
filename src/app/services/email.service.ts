import { Injectable } from '@angular/core';

@Injectable()
export class ServiceNameService {
    // When finished it should email the score to the appropriate person with?
    // May be better to add the name to a database to be less anonying
    constructor() { }
}